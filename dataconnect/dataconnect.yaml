specVersion: "v1"
serviceId: "live-connect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "live-connect-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
